<div id="<%= dom_id doctor %>">
  <table>
    <tr>
      <td><%= doctor.fio %></td>
    </tr>
    <tr>
      <td><%=  Special.find(doctor.special_id).special %></td>
    </tr>
    <tr>
      <td><%=  Department.find(doctor.department_id).depart %></td>
    </tr>
  </table>
</div>
