<%= form_with(model: doctor) do |form| %>
  <% if doctor.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(doctor.errors.count, "error") %> prohibited this doctor from being saved:</h2>

      <ul>
        <% doctor.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <p>Doctor: <%= form.text_field :fio %></p>
  <p>Special: <%= form.collection_select :special_id, Special.order(:special), :id, :special %></p>
  <p>Department: <%= form.collection_select :department_id, Department.order(:depart), :id, :depart %></p>
  <div>
    <%= form.submit %>
  </div>
<% end %>
